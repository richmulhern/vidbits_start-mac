    <div id="videos-container" class="contents-container">
      {{#each videos as |video|}}
        <div class="video-card">
          <div class="video-title">
            {{video.title}}
          </div>
        </div>
      {{/each}}
    </div>

    <a href="videos/create" class="add-video">Add Video</a>